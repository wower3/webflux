<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chat.chart.infrastructure.mapper.StatMapper">

    <!-- 统计用户量（按conversation_id去重，仅成功回复） -->
    <select id="selectUserCount" resultType="int">
        SELECT COUNT(DISTINCT conversation_id)
        FROM chat_message
        <where>
            role = 'assistant'
            AND is_success = '1'
            <if test="startTime != null">
                AND created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_at &lt; #{endTime}
            </if>
        </where>
    </select>

    <!-- 统计交易量（成功的模型回复数） -->
    <select id="selectTransactionCount" resultType="int">
        SELECT COUNT(*)
        FROM chat_message
        <where>
            role = 'assistant'
            AND is_success = '1'
            <if test="startTime != null">
                AND created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_at &lt; #{endTime}
            </if>
        </where>
    </select>

    <!-- 统计未采纳数（仅成功回复） -->
    <select id="selectNotAdoptedCount" resultType="int">
        SELECT COUNT(*)
        FROM chat_message
        <where>
            role = 'assistant'
            AND is_success = '1'
            AND adoption_status = #{notAdoptedStatus}
            <if test="startTime != null">
                AND created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_at &lt; #{endTime}
            </if>
        </where>
    </select>

</mapper>
